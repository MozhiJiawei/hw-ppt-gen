# Candidate Prompt: Huawei PPT Forward Test

Use the repository's Huawei PPT generation Skill to create a complete PPT deck from:

```text
candidate/input/ppt_content_brief.md
```

Write all generated artifacts under:

```text
.tmp/forward-tests/aegaeon-content-aware-layout/<run-id>/
```

Use a clear run id, for example `candidate-YYYYMMDD-HHMMSS`.

When finished, report the output directory, PPTX path, exported slide PNG directory, hard-QA status, visual-QA status, and caveats.
